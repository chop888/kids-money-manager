<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.CategoryMapper">

<!-- 全カテゴリー取得 -->
<select id="findAll" resultType="com.example.app.entity.Category"><!-- 戻り値：List＜Category＞ -->
	SELECT 
		category_id,
		category_name,
		record_type,
		display_order
	FROM categories
	ORDER BY record_type, display_order
</select>

<!-- レコードタイプ別カテゴリー取得 -->
<select id="findByRecordType" resultType="com.example.app.entity.Category">
	SELECT
		category_id,
		category_name,
		record_type,
		display_order
	FROM categories
	WHERE record_type = #{recordType}
	ORDER BY display_order
</select>

</mapper>